## VS Code Workflow

### Open Project + Run Commands
```
Open project folder
Open terminal
git add .
git commit -m "message"
git push origin main
```

### Branch
```bash
git checkout -b feature-name
```

### Merge
Use GitHub Pull Request (recommended)
